package inheritance_has_07_0213;

public class Driver {
	//메소드 선언(클래스 타입의 매개변수를 가지고 있음)
	public void driver(Vehicle vehicle) {
		vehicle.run(); // Vehicle vehicle = bus;
								 // Vehicle vehicle = taxi;
	}
}
